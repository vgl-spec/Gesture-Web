{
  "prompt": "Create a complete hand gesture recognition web application with the following comprehensive features:\n\n1. CAMERA INTEGRATION:\n   - Access user's webcam (front-facing camera)\n   - Display live video feed in real-time\n   - Handle camera permissions and errors gracefully\n   - Show status messages for camera state\n   - Implement start/stop camera controls\n\n2. MEDIAPIPE HANDS INTEGRATION:\n   - Import MediaPipe Hands library via CDN (https://cdn.jsdelivr.net/npm/@mediapipe/hands/hands.js)\n   - Initialize hand detection with proper configuration:\n     * maxNumHands: 1\n     * modelComplexity: 1\n     * minDetectionConfidence: 0.5\n     * minTrackingConfidence: 0.5\n   - Detect 21 hand landmarks in real-time\n   - Process hand detection at 10 FPS (100ms intervals)\n   - Handle cases when no hand is detected\n\n3. HAND SKELETON VISUALIZATION:\n   - Draw all 21 hand landmarks as red circles (5px radius)\n   - Display landmark numbers (0-20) next to each point\n   - Connect landmarks with colored lines showing hand structure:\n     * Thumb (landmarks 0-4): Green (#00ff00)\n     * Index finger (landmarks 5-8): Blue (#0088ff)\n     * Middle finger (landmarks 9-12): Magenta (#ff00ff)\n     * Ring finger (landmarks 13-16): Yellow (#ffff00)\n     * Pinky finger (landmarks 17-20): Orange (#ff8800)\n     * Palm connections (5-9, 9-13, 13-17): White (#ffffff)\n   - Line width: 3px\n   - Overlay skeleton on video feed using canvas\n\n4. TRAINING MODE INTERFACE:\n   - Toggle button to switch between Training Mode and Detection Mode\n   - Training Mode features:\n     * 'Capture Screenshot' button to freeze current video frame\n     * Display captured screenshot in training panel\n     * 'Save as Open Palm' button\n     * 'Save as Closed Fist' button\n     * Cancel button to discard screenshot\n   - Store training samples with:\n     * Hand landmark coordinates (all 21 points)\n     * Extracted feature vectors\n     * Screenshot image thumbnail\n     * Timestamp\n\n5. FEATURE EXTRACTION ALGORITHM:\n   - Calculate for each finger (thumb, index, middle, ring, pinky):\n     * Average angle between consecutive joints\n     * Use 3-point angle calculation (previous-current-next joint)\n   - Calculate distances from all 5 fingertips to palm center (landmark 0)\n   - Measure palm size (distance from wrist to middle finger base)\n   - Normalize all distances by palm size for scale invariance\n   - Store features as vectors for comparison\n\n6. GESTURE DETECTION ALGORITHM:\n   - Extract features from live hand landmarks every frame\n   - Compare current features against all saved training samples:\n     * For each gesture type (open palm, closed fist)\n     * Calculate similarity score using:\n       - Finger angle differences (weight: 2x)\n       - Normalized fingertip distance differences (weight: 1x)\n     * Average similarity across all samples of each gesture\n   - Select gesture with highest similarity score\n   - Only recognize gesture if confidence exceeds sensitivity threshold\n   - Display gesture name and confidence percentage\n\n7. TRAINING SAMPLE MANAGEMENT:\n   - Display two columns: 'Open Palm Samples' and 'Closed Fist Samples'\n   - Show thumbnail preview for each sample (64x64px)\n   - Display sample count for each gesture type\n   - Delete button for each sample with trash icon\n   - Store samples in component state (React useState)\n\n8. THREE.JS PARTICLE SYSTEM:\n   - Import Three.js r128 via CDN (https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js)\n   - Create 3D scene with:\n     * PerspectiveCamera (FOV: 75, position z: 5)\n     * WebGLRenderer with antialias and alpha\n     * Square canvas (400x400px or responsive)\n   - Create 200 particles arranged in car shape:\n     * 50 particles for main body (2x0.8x1 units)\n     * 30 particles for cabin (1.2x0.6x0.8 units, elevated)\n     * 120 particles for wheels (2 circular wheels, radius 0.3)\n   - Particle properties:\n     * Size: 0.05 units\n     * Colors: Blue gradient (vertex colors)\n     * Point material\n   - Continuous rotation: 0.005 radians/frame around Y-axis\n\n9. PARTICLE PHYSICS AND ANIMATIONS:\n   - Store original position for each particle\n   - Track velocity (vx, vy, vz) for each particle\n   - Explosion effect (triggered by Open Palm gesture):\n     * Assign random velocity to each particle (-0.1 to +0.1 per axis)\n     * Update positions each frame: position += velocity\n     * Apply damping: velocity *= 0.98\n     * Set 'exploded' flag to true\n   - Reassembly effect (triggered by Closed Fist gesture):\n     * Calculate velocity toward original position: v = (original - current) * 0.1\n     * Apply spring-like motion\n     * After 2 seconds, snap particles to exact original positions\n     * Set 'exploded' flag to false\n   - Prevent repeated triggers if already in same state\n\n10. USER INTERFACE LAYOUT:\n   - Dark theme (gray-900 background)\n   - Responsive grid layout:\n     * Desktop: 2 columns (camera + 3D car side by side)\n     * Mobile: 1 column (stacked)\n   - Camera section (left/top):\n     * 400px height video display with canvas overlay\n     * Camera placeholder icon when inactive\n     * Control buttons (Start/Stop Camera, Mode Toggle)\n     * Capture Screenshot button (training mode only)\n     * Status panel showing:\n       - Current status message\n       - Hand detection indicator (green/gray hand icon)\n       - Detected gesture name (highlighted when active)\n       - Confidence percentage\n     * Sensitivity slider (0-100%, default 70%)\n   - 3D Car section (right/bottom):\n     * Title: 'Particle Car'\n     * Three.js canvas (400x400px)\n     * Legend: '✋ Open Palm = Explode' and '✊ Closed Fist = Reassemble'\n   - Training panel (full width below, training mode only):\n     * Screenshot preview (centered, max-width 400px)\n     * Save buttons (Open Palm, Closed Fist, Cancel)\n     * Two-column sample display\n   - Instructions panel (full width bottom):\n     * Step-by-step usage guide\n\n11. STATE MANAGEMENT:\n   - React component with useState hooks for:\n     * isTrainingMode (boolean)\n     * cameraActive (boolean)\n     * handDetected (boolean)\n     * currentGesture (string: 'None', 'Open Palm', 'Closed Fist')\n     * confidence (number: 0-1)\n     * trainingSamples (object with openPalm and closedFist arrays)\n     * screenshot (string: base64 image data)\n     * sensitivity (number: 0-1, default 0.7)\n     * status (string: current status message)\n   - useRef hooks for:\n     * videoRef (video element)\n     * canvasRef (canvas element)\n     * threeContainerRef (Three.js container)\n     * handsRef (MediaPipe Hands instance)\n     * threeSceneRef (Three.js scene objects)\n     * particlesRef (particle data)\n     * animationFrameRef (animation loop ID)\n     * detectionIntervalRef (detection interval ID)\n\n12. ERROR HANDLING:\n   - Camera access errors: Display error message with reason\n   - MediaPipe loading errors: Show initialization status\n   - No hand detected: Clear gesture display, show 'No Hand' status\n   - No training data: Display 'No training data' message\n   - Graceful degradation for missing features\n\n13. PERFORMANCE OPTIMIZATIONS:\n   - Hand detection at 10 FPS (not 60 FPS) to reduce CPU usage\n   - Canvas cleared and redrawn only when hand detected\n   - Three.js animation runs at 60 FPS independently\n   - Particle updates only when exploded flag is true\n   - Efficient feature comparison algorithm\n\n14. VISUAL FEEDBACK:\n   - Color-coded buttons:\n     * Green: Start Camera\n     * Red: Stop Camera\n     * Orange: Training Mode active\n     * Blue: Detection Mode active\n     * Purple: Capture Screenshot\n   - Hand detection indicator changes color (green = detected, gray = not detected)\n   - Gesture name highlighted in green when detected\n   - Confidence percentage updates in real-time\n   - Status messages update for all actions\n\n15. TECHNICAL REQUIREMENTS:\n   - React functional component with hooks\n   - Tailwind CSS for styling (utility classes only)\n   - Lucide React icons: Camera, Hand, Save, Trash2, Play, Square\n   - MediaPipe Hands library for hand tracking\n   - Three.js r128 for 3D graphics\n   - Canvas API for drawing hand skeleton\n   - No localStorage or sessionStorage (React state only)\n   - Proper cleanup on component unmount:\n     * Stop camera tracks\n     * Cancel animation frames\n     * Clear intervals\n     * Remove Three.js renderer\n\n16. USER WORKFLOW:\n   Step 1: Click 'Start Camera' → Camera activates, hand tracking begins\n   Step 2: Click 'Training Mode' → Enables screenshot capture\n   Step 3: Show open palm gesture → Ensure hand landmarks visible\n   Step 4: Click 'Capture Screenshot' → Image captured\n   Step 5: Click 'Save as Open Palm' → Sample saved with features\n   Step 6: Repeat steps 3-5 for open palm (3-5 samples recommended)\n   Step 7: Show closed fist gesture → Ensure hand landmarks visible\n   Step 8: Click 'Capture Screenshot' → Image captured\n   Step 9: Click 'Save as Closed Fist' → Sample saved with features\n   Step 10: Repeat steps 7-9 for closed fist (3-5 samples recommended)\n   Step 11: Click 'Detection Mode' → Activates real-time gesture recognition\n   Step 12: Show trained gestures → Particles respond accordingly\n   Step 13: Adjust sensitivity slider if needed for better detection\n\n17. MATHEMATICAL FORMULAS:\n   - Angle between three points (p1, p2, p3):\n     * v1 = (p1.x - p2.x, p1.y - p2.y)\n     * v2 = (p3.x - p2.x, p3.y - p2.y)\n     * angle = arccos(dot(v1,v2) / (magnitude(v1) * magnitude(v2))) * 180/π\n   - Distance between two points:\n     * distance = sqrt((p2.x - p1.x)² + (p2.y - p1.y)²)\n   - Feature similarity:\n     * angle_similarity = max(0, 1 - |angle_diff| / 180)\n     * distance_similarity = max(0, 1 - |normalized_distance_diff|)\n     * total_score = (sum(angle_similarities * 2) + sum(distance_similarities)) / total_weights\n     * gesture_confidence = average(scores_from_all_samples)\n\nBuild a complete, production-ready React component with all features fully implemented. The application should be intuitive, visually polished, and provide clear feedback at every step. Ensure robust error handling and smooth animations. The gesture recognition should be accurate and responsive when properly trained.",
  "instructions": "Create a single React component that implements all features. Use CDN imports for MediaPipe Hands (https://cdn.jsdelivr.net/npm/@mediapipe/hands/hands.js) and Three.js r128 (https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js). Implement proper initialization sequences, error handling, and cleanup. The hand skeleton should be clearly visible with color-coded fingers. Training should allow multiple samples per gesture. Detection algorithm should compare features using weighted similarity scoring. Particle effects should be smooth and responsive to gestures. UI should be clean with Tailwind styling. Include comprehensive status messages and visual feedback throughout the user journey."
}